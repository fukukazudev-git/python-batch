[tool.ruff]
target-version = "py311"
line-length = 100

[tool.ruff.lint]
# E,F: pycodestyle+pyflakes / I: isort / UP: pyupgrade / B: bugbear
select = ["E", "F", "I", "UP", "B"]

[tool.ruff.format]
# ruff format は black 互換。既定設定のまま使う
